// Generated by scripts/generate-showcase-contract.mjs. Do not edit.
export const contractName = "RenderAdapter";
export const typeFields = Object.freeze({
  "JsonValue": "recursive-union",
  "JsonObject": "alias",
  "DefineRegistry": "alias",
  "DefineEntry": "union",
  "Environment": [
    "timezone",
    "now"
  ],
  "Scenario": [
    "target",
    "assign",
    "define",
    "env"
  ],
  "RenderRequest": [
    "target",
    "assign",
    "define",
    "env"
  ],
  "RepeatResult": [
    "first",
    "second"
  ]
});
export const operations = Object.freeze(["loadScenario","buildRequest","render","renderTwice"]);

export function assertRenderAdapter(adapter) {
  for (const operation of operations) {
    if (typeof adapter[operation] !== "function") throw new Error("missing adapter operation: " + operation);
  }
  return adapter;
}

export function assertRequestShape(request) {
  const keys = Object.keys(request);
  const required = ["target","assign","define"];
  const optional = ["env"];
  const expected = Object.hasOwn(request, "env") ? required.concat(optional) : required;
  if (keys.join(",") !== expected.join(",")) {
    throw new Error("request fields do not follow the manifest order");
  }
  return request;
}
