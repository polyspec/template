{
  "schema": 1,
  "composerPlatforms": [
    {
      "manifest": "packages/template-php/composer.json",
      "php": "8.2.0"
    },
    {
      "manifest": "packages/template-php-ext/composer.json",
      "php": "8.2.0"
    }
  ],
  "exceptions": [
    {
      "ecosystem": "npm",
      "manifest": "package.json",
      "package": "esbuild",
      "reason": "The latest stable release removes transforms required by the current package and documentation build chains.",
      "removalCondition": "Remove the pin when the latest stable release is accepted by both build chains and make test-ts docs-verify-idempotent passes.",
      "verification": ["make test-ts", "make docs-verify-idempotent"]
    },
    {
      "ecosystem": "npm",
      "manifest": "package.json",
      "package": "typescript",
      "reason": "The latest stable release is outside the current lint peer contract and breaks declaration generation.",
      "removalCondition": "Remove the pin when the lint peer contract accepts the release and make test-ts passes with declaration output enabled.",
      "verification": ["make test-ts"]
    }
  ]
}
